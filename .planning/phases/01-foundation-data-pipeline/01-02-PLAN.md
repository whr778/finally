---
phase: 01-foundation-data-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - frontend/src/app/page.tsx
  - frontend/src/components/app-shell.tsx
  - frontend/src/components/header.tsx
  - frontend/src/components/connection-dot.tsx
  - frontend/src/components/placeholder-panel.tsx
  - frontend/src/components/trade-bar.tsx
  - frontend/src/components/chat-drawer.tsx
  - frontend/src/components/chat-input.tsx
autonomous: false
requirements:
  - RTD-03
  - UI-03

must_haves:
  truths:
    - "App renders in browser with dark terminal theme and all panel placeholders visible"
    - "Connection status indicator shows green dot + LIVE when SSE is connected"
    - "Connection status indicator shows yellow dot + CONNECTING when reconnecting"
    - "Connection status indicator shows red dot + OFFLINE when disconnected"
    - "Header displays portfolio total value formatted as $10,000.00"
    - "Header displays cash balance formatted as $10,000.00"
    - "Trade bar shows disabled ticker input, qty input, buy button, sell button"
    - "Chat panel is visible with AI Assistant heading and disabled chat input"
    - "All 10 data-testid attributes from E2E contract are present in DOM"
  artifacts:
    - path: "frontend/src/components/app-shell.tsx"
      provides: "Main layout: header + CSS grid body + chat drawer"
      contains: "'use client'"
    - path: "frontend/src/components/header.tsx"
      provides: "Logo, total value, cash balance, connection dot, chat toggle"
      contains: "data-testid"
    - path: "frontend/src/components/connection-dot.tsx"
      provides: "SSE status indicator (dot + label)"
      contains: "connection-dot"
    - path: "frontend/src/components/placeholder-panel.tsx"
      provides: "Reusable bordered panel with title and placeholder text"
      contains: "bg-bg-panel"
    - path: "frontend/src/components/trade-bar.tsx"
      provides: "Disabled trade inputs placeholder"
      contains: "trade-bar"
    - path: "frontend/src/components/chat-drawer.tsx"
      provides: "Collapsible right-side chat panel"
      contains: "chat-panel"
    - path: "frontend/src/components/chat-input.tsx"
      provides: "Disabled chat text input"
      contains: "chat-input"
  key_links:
    - from: "frontend/src/components/app-shell.tsx"
      to: "frontend/src/hooks/use-sse.ts"
      via: "calls useSSE() to establish SSE connection"
      pattern: "useSSE"
    - from: "frontend/src/components/app-shell.tsx"
      to: "frontend/src/stores/portfolio-store.ts"
      via: "calls fetchPortfolio() on mount"
      pattern: "fetchPortfolio"
    - from: "frontend/src/components/header.tsx"
      to: "frontend/src/stores/portfolio-store.ts"
      via: "reads cashBalance and totalValue"
      pattern: "usePortfolioStore"
    - from: "frontend/src/components/connection-dot.tsx"
      to: "frontend/src/stores/price-store.ts"
      via: "reads connectionStatus via useConnectionStatus()"
      pattern: "useConnectionStatus"
---

<objective>
Build all UI components for the Phase 1 layout shell: AppShell with CSS Grid, Header with portfolio values and connection indicator, PlaceholderPanels for future content areas, TradeBar (disabled), ChatDrawer (visible by default), and ConnectionDot. Wire the SSE hook and portfolio fetch into the app shell.

Purpose: Delivers the complete visible UI that satisfies all Phase 1 E2E test contracts and gives the user a working dark terminal interface with live connection status.
Output: Fully rendered app shell with all panels, header values, and SSE connection indicator working against the backend.
</objective>

<execution_context>
@/Users/williamroe/development/projects/agenic_coder/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/williamroe/development/projects/agenic_coder/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-pipeline/01-RESEARCH.md
@.planning/phases/01-foundation-data-pipeline/01-PATTERNS.md
@.planning/phases/01-foundation-data-pipeline/01-UI-SPEC.md
@.planning/phases/01-foundation-data-pipeline/01-01-SUMMARY.md

<interfaces>
<!-- Types and stores created in Plan 01 that this plan uses directly. -->

From frontend/src/types/market.ts:
```typescript
export interface PriceData {
  ticker: string
  price: number
  previous_price: number
  timestamp: number
  change: number
  change_percent: number
  direction: 'up' | 'down' | 'flat'
}
export interface PositionData { ... }
export interface PortfolioData {
  cash_balance: number
  positions: PositionData[]
  total_value: number
}
export interface WatchlistEntryData { ... }
```

From frontend/src/stores/price-store.ts:
```typescript
export const usePriceStore = create<PriceState>()(...)
export const useTickerPrice = (ticker: string) => usePriceStore((state) => state.prices[ticker])
export const useConnectionStatus = () => usePriceStore((state) => state.connectionStatus)
```

From frontend/src/stores/portfolio-store.ts:
```typescript
export const usePortfolioStore = create<PortfolioState>()(...)
// State: cashBalance, totalValue, positions
// Actions: fetchPortfolio()
```

From frontend/src/hooks/use-sse.ts:
```typescript
export function useSSE(): void
```

E2E test contract (required data-testid attributes):
| data-testid      | Element                    | Content Requirement              |
|------------------|----------------------------|----------------------------------|
| connection-dot   | Connection status indicator | Contains "LIVE" when connected   |
| total-value      | Portfolio total value       | Contains "10,000"                |
| cash-balance     | Cash balance                | Contains "10,000"                |
| trade-bar        | Trade bar container         | Visible                          |
| trade-ticker     | Ticker input                | Visible                          |
| trade-qty        | Quantity input              | Visible                          |
| btn-buy          | Buy button                  | Visible                          |
| btn-sell         | Sell button                 | Visible                          |
| chat-panel       | Chat panel                  | Visible                          |
| chat-input       | Chat message input          | Visible                          |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ConnectionDot, PlaceholderPanel, TradeBar, ChatInput, and ChatDrawer components</name>
  <files>
    frontend/src/components/connection-dot.tsx
    frontend/src/components/placeholder-panel.tsx
    frontend/src/components/trade-bar.tsx
    frontend/src/components/chat-input.tsx
    frontend/src/components/chat-drawer.tsx
  </files>
  <read_first>
    frontend/src/stores/price-store.ts
    frontend/src/app/globals.css
    .planning/phases/01-foundation-data-pipeline/01-UI-SPEC.md
    .planning/phases/01-foundation-data-pipeline/01-PATTERNS.md
    test/specs/startup.spec.ts
  </read_first>
  <action>
1. Create `frontend/src/components/connection-dot.tsx` (per D-06, D-07):
   - `'use client'` directive at top.
   - Import `useConnectionStatus` from `@/stores/price-store`.
   - Define a `STATUS_CONFIG` constant mapping:
     - `connected` -> `{ color: 'bg-success', label: 'LIVE' }`
     - `reconnecting` -> `{ color: 'bg-warning', label: 'CONNECTING' }`
     - `disconnected` -> `{ color: 'bg-danger', label: 'OFFLINE' }`
   - Render: a `<div data-testid="connection-dot" className="flex items-center gap-1">` containing:
     - `<span>` with classes `size-2 rounded-full` plus the dynamic color class from STATUS_CONFIG
     - `<span>` with classes `text-xs text-text-muted` containing the label text
   - The `size-2` class produces an 8px dot (Tailwind 4: `0.5rem = 8px`).

2. Create `frontend/src/components/placeholder-panel.tsx`:
   - No `'use client'` needed (pure presentational, no hooks).
   - Props: `title: string`, `phaseNote: string` (e.g., "Coming in Phase 2").
   - Render: `<div>` with classes `bg-bg-panel rounded-lg p-4` and panel glow styling applied via inline style:
     ```
     style={{ border: '1px solid rgba(125,133,144,0.2)', boxShadow: '0 0 0 1px rgba(32,157,215,0.15), 0 0 8px rgba(32,157,215,0.08)' }}
     ```
   - Inside: `<h2>` with classes `text-base font-semibold text-text-primary mb-2` for the title, and `<p>` with classes `text-xs text-text-muted` for the phaseNote.

3. Create `frontend/src/components/trade-bar.tsx`:
   - `'use client'` directive.
   - Render: a `<div data-testid="trade-bar">` with classes `bg-bg-panel rounded-lg p-3 flex items-center gap-3` and the same panel glow inline style.
   - Inside:
     - `<input data-testid="trade-ticker" type="text" placeholder="Ticker" disabled className="..." />` -- styled with `bg-bg-primary text-text-primary font-mono text-sm rounded px-2 py-1.5 w-24 disabled:opacity-50 border border-border-subtle`
     - `<input data-testid="trade-qty" type="number" placeholder="Qty" disabled className="..." />` -- same styling, `w-20`
     - `<button data-testid="btn-buy" disabled className="...">Buy</button>` -- styled with `bg-success/50 text-text-primary font-semibold text-sm rounded px-4 py-1.5 disabled:opacity-50`
     - `<button data-testid="btn-sell" disabled className="...">Sell</button>` -- styled with `bg-danger/50 text-text-primary font-semibold text-sm rounded px-4 py-1.5 disabled:opacity-50`

4. Create `frontend/src/components/chat-input.tsx`:
   - `'use client'` directive.
   - Render: an `<input data-testid="chat-input" type="text" placeholder="Ask your AI assistant..." disabled className="..." />` -- styled with `w-full bg-bg-primary text-text-primary text-sm rounded-lg px-3 py-2 border border-border-subtle disabled:opacity-50 font-sans`.

5. Create `frontend/src/components/chat-drawer.tsx`:
   - `'use client'` directive.
   - Props: `open: boolean`, `onToggle: () => void`.
   - When `open` is true, render a `<div data-testid="chat-panel">` with classes for the right-side fixed drawer:
     `fixed right-0 top-12 w-[360px] h-[calc(100vh-48px)] bg-bg-panel z-20 flex flex-col` plus panel glow inline style.
   - Inside the drawer:
     - Header row: `<div className="flex items-center justify-between p-4 border-b border-border-subtle">` with `<h2 className="text-base font-semibold">AI Assistant</h2>` and a close button `<button onClick={onToggle} className="text-text-muted hover:text-text-primary text-sm">Close</button>`.
     - Message area: `<div className="flex-1 p-4 overflow-y-auto">` (empty in Phase 1).
     - Input area: `<div className="p-4 border-t border-border-subtle">` containing the `<ChatInput />` component.
   - When `open` is false, render the `<div data-testid="chat-panel">` with `className="hidden"` (not visible). The parent AppShell must pass `open={true}` initially so chat-panel is visible at startup per E2E test requirements.
  </action>
  <verify>
    <automated>cd /Users/williamroe/development/projects/agenic_coder/finally/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/connection-dot.tsx` contains `data-testid="connection-dot"` and `LIVE` and `CONNECTING` and `OFFLINE`
    - `frontend/src/components/trade-bar.tsx` contains `data-testid="trade-bar"` and `data-testid="trade-ticker"` and `data-testid="trade-qty"` and `data-testid="btn-buy"` and `data-testid="btn-sell"`
    - `frontend/src/components/chat-drawer.tsx` contains `data-testid="chat-panel"` and `AI Assistant`
    - `frontend/src/components/chat-input.tsx` contains `data-testid="chat-input"` and `Ask your AI assistant`
    - `frontend/src/components/placeholder-panel.tsx` contains `bg-bg-panel` and `rounded-lg`
    - All five component files contain valid TSX that compiles without type errors
  </acceptance_criteria>
  <done>Five leaf components created: ConnectionDot with three states per D-06/D-07, PlaceholderPanel with glow styling per D-03, TradeBar with all four testid inputs disabled, ChatDrawer with AI Assistant heading visible by default, ChatInput with placeholder text. All compile without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create Header, AppShell, and wire page.tsx with SSE and portfolio fetch</name>
  <files>
    frontend/src/components/header.tsx
    frontend/src/components/app-shell.tsx
    frontend/src/app/page.tsx
  </files>
  <read_first>
    frontend/src/stores/portfolio-store.ts
    frontend/src/stores/price-store.ts
    frontend/src/hooks/use-sse.ts
    frontend/src/components/connection-dot.tsx
    frontend/src/components/placeholder-panel.tsx
    frontend/src/components/trade-bar.tsx
    frontend/src/components/chat-drawer.tsx
    frontend/src/components/chat-input.tsx
    frontend/src/app/globals.css
    .planning/phases/01-foundation-data-pipeline/01-UI-SPEC.md
    test/specs/startup.spec.ts
  </read_first>
  <action>
1. Create `frontend/src/components/header.tsx`:
   - `'use client'` directive.
   - Import `usePortfolioStore` from `@/stores/portfolio-store`, `ConnectionDot` from `@/components/connection-dot`.
   - Create a `formatCurrency` helper using `Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', minimumFractionDigits: 2, maximumFractionDigits: 2 })`.
   - Subscribe to `cashBalance` and `totalValue` from portfolioStore via selectors: `usePortfolioStore((s) => s.cashBalance)` and `usePortfolioStore((s) => s.totalValue)`.
   - Render a `<header>` element with classes `h-12 bg-bg-panel flex items-center justify-between px-4 sticky top-0 z-10` and panel glow border-bottom style: `style={{ borderBottom: '1px solid rgba(125,133,144,0.2)' }}`.
   - Left side: Logo -- `<span className="text-xl font-semibold">` containing `<span className="text-text-primary">Fin</span><span className="text-accent-yellow">Ally</span>`.
   - Right side: `<div className="flex items-center gap-4">` containing:
     - Total value: `<span data-testid="total-value" className="text-xl font-semibold font-sans">{formatCurrency(totalValue)}</span>`
     - Cash section: `<div className="flex items-center gap-1"><span className="text-xs text-text-muted">Cash</span><span data-testid="cash-balance" className="text-sm font-mono">{formatCurrency(cashBalance)}</span></div>`
     - `<ConnectionDot />`
     - Chat toggle button (receives `onChatToggle` prop): `<button onClick={onChatToggle} className="text-xs text-text-muted hover:text-accent-blue px-2 py-1 rounded border border-border-subtle">Chat</button>`
   - Props: `onChatToggle: () => void`.

2. Create `frontend/src/components/app-shell.tsx`:
   - `'use client'` directive.
   - Import: `useSSE` from `@/hooks/use-sse`, `usePortfolioStore` from `@/stores/portfolio-store`, `Header`, `PlaceholderPanel`, `TradeBar`, `ChatDrawer`, and `{ useEffect, useState }` from React.
   - Call `useSSE()` at the top (establishes SSE connection for lifetime of the component).
   - Call `usePortfolioStore((s) => s.fetchPortfolio)` to get the action, then `useEffect(() => { fetchPortfolio() }, [fetchPortfolio])` to fetch portfolio data on mount.
   - Manage chat drawer state: `const [chatOpen, setChatOpen] = useState(true)` -- starts open per E2E requirement.
   - Render layout structure (per D-01, UI-03):
     ```
     <div className="h-screen flex flex-col overflow-hidden">
       <Header onChatToggle={() => setChatOpen(!chatOpen)} />
       <div className="flex-1 grid grid-cols-[280px_1fr] grid-rows-[1fr_auto_auto] gap-4 p-4 overflow-hidden">
         {/* Top left: Watchlist */}
         <PlaceholderPanel title="Watchlist" phaseNote="Coming in Phase 2" />
         {/* Top right: Chart */}
         <PlaceholderPanel title="Chart" phaseNote="Coming in Phase 4" />
         {/* Bottom row: 3 panels */}
         <div className="col-span-2 grid grid-cols-3 gap-4">
           <PlaceholderPanel title="Positions" phaseNote="Coming in Phase 3" />
           <PlaceholderPanel title="Portfolio Map" phaseNote="Coming in Phase 4" />
           <PlaceholderPanel title="P&amp;L" phaseNote="Coming in Phase 4" />
         </div>
         {/* Trade bar: full width */}
         <div className="col-span-2">
           <TradeBar />
         </div>
       </div>
       <ChatDrawer open={chatOpen} onToggle={() => setChatOpen(!chatOpen)} />
     </div>
     ```

3. Update `frontend/src/app/page.tsx`:
   - Import `AppShell` from `@/components/app-shell`.
   - Render: `export default function Home() { return <AppShell /> }`
   - Remove the previous placeholder content.

4. Build and verify:
   ```bash
   cd frontend && npm run build
   ```
  </action>
  <verify>
    <automated>cd /Users/williamroe/development/projects/agenic_coder/finally/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/header.tsx` contains `data-testid="total-value"` and `data-testid="cash-balance"` and `Fin` and `Ally` and `text-accent-yellow`
    - `frontend/src/components/header.tsx` contains `Intl.NumberFormat`
    - `frontend/src/components/app-shell.tsx` contains `useSSE()` and `fetchPortfolio` and `useState(true)` (chat starts open)
    - `frontend/src/components/app-shell.tsx` contains `grid-cols-[280px_1fr]`
    - `frontend/src/app/page.tsx` imports and renders `AppShell`
    - `npm run build` completes without errors
    - All 10 data-testid values from E2E contract appear in the component source files: `connection-dot`, `total-value`, `cash-balance`, `trade-bar`, `trade-ticker`, `trade-qty`, `btn-buy`, `btn-sell`, `chat-panel`, `chat-input`
  </acceptance_criteria>
  <done>Header shows formatted portfolio values and connection status. AppShell renders full CSS grid layout with header, 5 placeholder panels, trade bar, and chat drawer. SSE hook wired at app-shell level. Portfolio data fetched on mount. Chat panel visible by default. All E2E data-testid attributes present. Project builds to static export.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of dark terminal layout</name>
  <files>frontend/src/app/page.tsx</files>
  <action>
Human verifies the visual output of the complete Phase 1 frontend. All automated work is complete in Tasks 1 and 2. This checkpoint confirms the dark terminal aesthetic, layout structure, and live data connections look correct in a real browser.
  </action>
  <verify>
    <automated>cd /Users/williamroe/development/projects/agenic_coder/finally/frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <how-to-verify>
    1. Start the backend: `cd backend && uv run uvicorn app.main:app --host 0.0.0.0 --port 8000`
    2. In another terminal, start the frontend dev server: `cd frontend && npm run dev`
    3. Open `http://localhost:3000` in a browser (Next.js dev server proxies API calls -- if proxy is not configured, you may need to set up a proxy in next.config.ts or access port 8000 directly after building)
    4. Verify you see:
       - Dark background (#0d1117) across the page
       - "FinAlly" logo with "Fin" in white and "Ally" in yellow (#ecad0a)
       - "$10,000.00" total value in the header
       - "Cash $10,000.00" in the header
       - Green dot with "LIVE" text in the header (connection indicator)
       - Grid layout with bordered panels: Watchlist, Chart, Positions, Portfolio Map, P&L
       - Each panel has a soft blue glow border effect
       - Trade bar at the bottom with disabled Ticker input, Qty input, Buy, Sell buttons
       - Chat drawer open on the right with "AI Assistant" heading and disabled input
       - Monospace font on data values, sans-serif on labels
  </how-to-verify>
  <acceptance_criteria>
    - User confirms visual layout matches the dark trading terminal design
    - User confirms all panels are visible with correct titles
    - User confirms connection indicator shows "LIVE" with green dot
    - User confirms header shows formatted dollar values
  </acceptance_criteria>
  <done>User has visually confirmed the Phase 1 layout renders correctly in the browser with dark theme, all panels, header values, and live connection status.</done>
  <resume-signal>Type "approved" if the layout looks correct, or describe any issues you see.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE stream -> React components | Price data flows from EventSource through Zustand store to UI components |
| Portfolio API -> Header values | Cash balance and total value rendered from API response |
| User input -> trade bar | All inputs disabled in Phase 1; no user input processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Spoofing | header.tsx portfolio values | accept | Same-origin API; single-user with simulated money; no real financial data |
| T-01-05 | Tampering | chat-input.tsx | accept | Input is disabled in Phase 1; no data sent to backend |
| T-01-06 | Information Disclosure | Portfolio values in DOM | accept | Single-user local app; no multi-user; simulated data only |
| T-01-07 | Denial of Service | SSE reconnection loop | mitigate | EventSource built-in retry with server-set 1000ms interval; browser handles backoff; no custom retry loop that could hammer the server |
</threat_model>

<verification>
1. `cd frontend && npm run build` succeeds
2. All 10 data-testid attributes from the E2E contract are present across component files
3. `grep -r "useSSE" frontend/src/components/app-shell.tsx` confirms SSE hook is wired
4. `grep -r "fetchPortfolio" frontend/src/components/app-shell.tsx` confirms portfolio fetch on mount
5. `grep "useState(true)" frontend/src/components/app-shell.tsx` confirms chat starts open
6. Visual inspection confirms dark theme, panel glow, correct fonts, and layout structure
</verification>

<success_criteria>
- All 7 components created and rendered in the layout
- Header displays formatted $10,000.00 values from portfolio API
- Connection dot shows LIVE/CONNECTING/OFFLINE based on SSE state
- CSS Grid layout matches the wireframe: header, 2-column body, bottom panels, trade bar
- Chat drawer visible by default with AI Assistant heading
- All E2E data-testid attributes present in DOM
- Project builds to static export without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-pipeline/01-02-SUMMARY.md`
</output>
